;;; fs-glsl.el --- glsl-mode setup

;;; Commentary:
;; 

;;; Code:
(require 'company-glsl)

;; (add-hook 'glsl-mode-hook (lambda ()
;; 			    (add-to-list 'company-backends 'company-glsl)))

(provide 'fs-glsl)

;;; fs-glsl.el ends here
