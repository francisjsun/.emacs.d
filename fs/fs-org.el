;;; fs-org.el --- org setup

;;; Commentary:
;;

(require 'org-indent)
(require 'ox-html)

;;; Code:

(setq org-startup-indented t)
(setq org-html-validation-link nil)

(provide 'fs-org)

;;; fs-org.el ends here
